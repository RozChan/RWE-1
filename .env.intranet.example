# Internal service URL used only by the Next.js server-side API proxy.
BACKEND_INTERNAL_URL=http://127.0.0.1:8000

# Listener settings consumed by the Windows intranet scripts.
FRONTEND_HOST=0.0.0.0
FRONTEND_PORT=3000
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
